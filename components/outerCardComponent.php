<style>
	.outerCard {
		padding: 0.5rem;
		margin: 5% 15%;
		min-height: 30vh;
		color: var(--buttonColor);
		box-shadow: 0 0.5rem 1rem 0.1rem var(--shadowColor);
		background: var(--outerCardBackgroundGradient);
		box-shadow: 0 0.5rem 1rem 0.1rem var(--shadowColor);
	}
</style>

<div class="outerCard">
	<?= $outerCardContent ?>
</div>